api_login_check:
    host: "api.{domain}"
    path: /login_check
    requirements:
        domain: "%domain%"
    defaults:
        domain: '%domain%'

pullup_front:
    resource: "@PullUpBundle/Resources/config/routing_front.yml"
    host: "{domain}"
    requirements:
        domain: "%domain%"
    defaults:
        domain: '%domain%'

pullup_api:
    resource: "@PullUpBundle/Resources/config/routing_api.yml"
    host: "api.{domain}"
    requirements:
        domain: "%domain%"
    defaults:
        domain: '%domain%'

umowieni_api_doc:
    resource: "@NelmioApiDocBundle/Resources/config/routing.yml"
    prefix:   /doc
    host: "api.{domain}"
    requirements:
        domain: "%domain%"
    defaults:
        domain: '%domain%'